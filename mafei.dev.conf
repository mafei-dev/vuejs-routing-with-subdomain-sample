server {
  listen 80;
  listen [::]:80;

  location / {
    root /var/www/html/front/dist;
    index index.html;
    error_page 404 $uri;
  }

  location /admin {
    alias /var/www/html/admin/dist;
    index index.html;
    error_page 404 $uri;
  }

  location /cashier {
    alias /var/www/html/cashier/dist;
    index index.html;
    error_page 404 $uri;
  }
}

